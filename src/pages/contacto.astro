---
import Layout from "../layouts/Layout.astro";

const subjects = [
  "Meeting Agenda",
  "Project Update",
  "Action Items",
  "New Hire Onboarding",
  "Budget Review",
  "Marketing Strategy",
  "Quarterly Report",
];
---

<Layout title="Flint - Ponte en contacto con nosotros">
  <!--<div
    class="w-[80%] lg:w-[35%] bg-zinc-500/75 backdrop-blur-lg mx-auto p-8 mt-10 rounded-lg"
  >
    <form
      autocomplete="off"
      action="https://formspree.io/f/xleyabzv"
      method="POST"
    >
      <div class="mb-5">
        <label for="name" class="mb-3 block text-base font-medium text-[#000]">
          Full Name
        </label>
        <input
          required
          type="text"
          name="name"
          id="name"
          placeholder="Full Name"
          class="w-full rounded-md border border-[#e0e0e0] bg-white py-3 px-6 text-base font-medium text-[#6B7280] outline-none focus:border-[#6A64F1] focus:shadow-md"
        />
      </div>
      <div class="mb-5">
        <label for="email" class="mb-3 block text-base font-medium text-[#000]">
          Email Address
        </label>
        <input
          required
          type="email"
          name="email"
          id="email"
          placeholder="example@domain.com"
          class="w-full rounded-md border border-[#e0e0e0] bg-white py-3 px-6 text-base font-medium text-[#6B7280] outline-none focus:border-[#6A64F1] focus:shadow-md"
        />
      </div>
      <div class="mb-5">
        <label
          for="subject"
          class="mb-3 block text-base font-medium text-[#000]"
          >Selecciona un subject:</label
        >
        <select
          required
          id="subject"
          name="subject"
          class="w-full text-gray-400 rounded-md border border-[#e0e0e0] bg-white py-3 px-6 text-base font-medium"
        >
          <option value="default" selected class="text-black"
            >Selecciona un subject</option
          >
          {
            subjects.map((subject) => (
              <option value={subject} class="text-black">
                {subject}
              </option>
            ))
          }
        </select>
      </div>
      <div class="mb-5">
        <label
          for="message"
          class="mb-3 block text-base font-medium text-[#000]"
        >
          Message
        </label>
        <textarea
          required
          rows="4"
          name="message"
          id="message"
          placeholder="Type your message"
          class="w-full resize-none rounded-md border border-[#e0e0e0] bg-white py-3 px-6 text-base font-medium text-[#6B7280] outline-none focus:border-[#6A64F1] focus:shadow-md"
        ></textarea>
      </div>
      <div class="text-center">
        <button
          type="submit"
          class="inline-flex items-center justify-center px-4 py-2 text-base
          font-bold leading-6 text-black bg-[#dec998]/75 ease-in duration-200 border border-transparent rounded-md
          hover:bg-zinc-800/75 hover:text-white focus:outline-none focus:ring-2 focus:ring-offset-2
          focus:bg-zinc-800/75"
        >
          Submit
        </button>
      </div>
    </form>
  </div>-->
  <section class="bg-[#1d1d1b]">
    <div class="py-8 lg:py-16 px-4 mx-auto max-w-screen-md">
      <h2
        class="mb-4 text-4xl tracking-tight font-extrabold text-center text-white"
      >
        Contact Us
      </h2>
      <p class="mb-8 lg:mb-16 font-light text-center text-gray-400 sm:text-xl">
        Got a technical issue? Want to send feedback about a beta feature? Need
        details about our Business plan? Let us know.
      </p>
      <form
        class="space-y-8"
        autocomplete="off"
        action="https://formspree.io/f/xleyabzv"
        method="POST"
      >
        <div>
          <label
            for="email"
            class="block mb-2 text-sm font-medium text-gray-300"
            >Your email</label
          >
          <input
            required
            type="email"
            name="email"
            id="email"
            class="shadow-sm border text-sm rounded-lg focus:ring-primary-500 focus:border-primary-500 block w-full p-2.5 bg-zinc-500 border-zinc-500 placeholder-gray-200 text-white focus:ring-primary-500 focus:border-primary-500 shadow-sm-light"
            placeholder="name@flowbite.com"
          />
        </div>

        <div class="mb-5">
          <label
            for="subject"
            class="block mb-2 text-sm font-medium text-gray-300"
            >Selecciona una opcion:</label
          >
          <select
            required
            id="subject"
            name="subject"
            class="block p-3 w-full text-sm bg-zinc-500 border-zinc-500 placeholder-gray-200 rounded-lg border shadow-sm focus:ring-primary-500 focus:border-primary-500"
          >
            <option value="default" selected class="text-gray-50"
              >Selecciona una</option
            >
            {
              subjects.map((subject) => (
                <option value={subject} class="text-gray-50">
                  {subject}
                </option>
              ))
            }
          </select>
        </div>
        <div>
          <label
            for="sub-subject"
            class="block mb-2 text-sm font-medium text-gray-300">Subject</label
          >
          <input
            type="text"
            id="sub-subject"
            name="sub-subject"
            class="block p-3 w-full text-sm rounded-lg border shadow-sm focus:ring-primary-500 focus:border-primary-500 bg-zinc-500 border-zinc-500 placeholder-gray-200 text-white focus:ring-primary-500 focus:border-primary-500 shadow-sm-light"
            placeholder="Let us know how we can help you"
            required
          />
        </div>
        <div class="sm:col-span-2">
          <label
            for="message"
            class="block mb-2 text-sm font-medium text-gray-200"
            >Your message</label
          >
          <textarea
            required
            name="message"
            id="message"
            rows="6"
            class="block p-2.5 w-full text-sm rounded-lg shadow-sm border focus:ring-primary-500 focus:border-primary-500 bg-zinc-500 border-zinc-500 placeholder-gray-200 text-white focus:ring-primary-500 focus:border-primary-500"
            placeholder="Leave a comment..."></textarea>
        </div>
        <button
          type="submit"
          class="py-3 px-5 text-sm font-medium text-center text-white rounded-lg bg-primary-700 sm:w-fit hover:bg-primary-800 focus:ring-4 focus:outline-none bg-primary-600 hover:bg-primary-700 focus:ring-primary-800"
          >Send message</button
        >
      </form>
    </div>
  </section>
</Layout>
